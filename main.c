#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	float a,b,c;
	printf( "nhap 2 so");
	scanf("%f %f",&a,&b);
	c = a-b;
	printf("ket qua %f",c);
	return 0;
}
